<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="preview.list">
      <field name="name">preview list</field>
      <field name="model">preview.preview</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="preview.action_window">
      <field name="name">preview window</field>
      <field name="res_model">preview.preview</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="preview.action_server">
      <field name="name">preview server</field>
      <field name="model_id" ref="model_preview_preview"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="preview" id="preview.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="preview.menu_1" parent="preview.menu_root"/>
    <menuitem name="Menu 2" id="preview.menu_2" parent="preview.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="preview.menu_1_list" parent="preview.menu_1"
              action="preview.action_window"/>
    <menuitem name="Server to list" id="preview" parent="preview.menu_2"
              action="preview.action_server"/>
    -->
  </data>
</odoo>